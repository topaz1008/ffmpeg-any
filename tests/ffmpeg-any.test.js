import { describe, expect, test } from '@jest/globals';

//import * as ffmpeg from '../ffmpeg-any';

describe('ffmpeg-any module', () => {
    test('tmp', () => {
        expect(1 + 1).toBe(2);
    });
});
